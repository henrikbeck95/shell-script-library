module randomTest {
    //The following should not be set a result test because they are random numbers
    random::generateNumberFromIntervalSet(1, 10);
    random::generateNumberFromIntervalSet(10, 1);
    random::generateNumberFromIntervalSet(-10, 1);
    random::generateNumberFromIntervalSet(1, -10);
}