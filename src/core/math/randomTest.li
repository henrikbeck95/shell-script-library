module randomTest {
    //The following should not be set a result test because they are random numbers
    random::generate_number_from_interval_set(1, 10);
    random::generate_number_from_interval_set(10, 1);
    random::generate_number_from_interval_set(-10, 1);
    random::generate_number_from_interval_set(1, -10);
}