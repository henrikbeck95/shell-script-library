module others {
    fn clock() {
        var aux: string = "";

        while true {
            sh {
                %aux=$(date)
            }

            print::defaultCarriageReturn(1, aux);
        }
    }
}